# ============================================================
# Smart Expense Splitter — Application Properties
# ============================================================

# Server
server.port=8080
server.address=0.0.0.0

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/expense_splitter?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=#Ranjana2127
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=YourSuperSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong!!
jwt.expiration=86400000

# Jackson
spring.jackson.serialization.fail-on-empty-beans=false
